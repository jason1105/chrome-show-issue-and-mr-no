{
  "name": "chrome-show-issue-and-mr-no",
  "version": "1.0.0",
  "private": true,
  "description": "Keep the current GitLab Issue or Merge Request number visible while scrolling.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "test:browser": "node tests/browser.test.js"
  }
}
