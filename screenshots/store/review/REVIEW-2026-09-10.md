# 商店截图复核报告 — 2026-09-10

**执行人**: UI
**基线**: GitHub main `bae57ac`（锚点校验通过，见下）
**范围**: 只改 `screenshots/store/`；未触碰 `src/`、`scripts/`、任何测试脚本
**任务**: 重截主面板 1 张（1280×800，深浅色按既有 5 张规格）+ 复核其余 4 张是否需更新

---

## 1. 结论一览

| 图 | 是否需换 | 原因 | 产出 |
|----|---------|------|------|
| 01 main-panel | **建议换（已就地替换）** | 分组标题文案变更；搜索框新增自动聚焦态 | `01-main-panel.jpg`（已替换，见 §3） |
| 02 state-filter | 可选替换（建议换） | 仅分组标题 `Issues`→`Issue`，其余像素级一致 | `review/02-panel-state-filter.zh.jpg` |
| 03 search | 可选替换（可不换） | 可见区域与旧图完全一致，仅整幅亮度差 | `review/03-panel-search.zh.png` |
| 04 options | **建议换** | 结构性过时：缺 `语言` 选项行；多处中文文案已改 | `review/04-options-page.zh-dark.png` |
| 05 badge | **不换** | 徽章区像素级一致（差异仅 1px 位移的抗锯齿） | `review/05-badge-scroll.zh.png`（仅留档） |

**「深色/浅色」口径确认**：既有 5 张**全部是深色面板**（深色面板 + 深色徽章 + 深色 Options 页，衬在浅色 GitLab 页面上）。因此新 01 也按**深色**出图，与既有规格一致；浅色版本只作对照留档。

---

## 2. 差异说明（旧图 → 新图）

### 2.1 全 5 张共有的真实产品变更

1. **面板分组标题文案**（唯一实质变更）
   - 第一组：`Issues` → `Issue`
   - 第二组：`Merge requests` → `合并请求`
   - 依据：`_locales/zh_CN/messages.json` 现为 `groupIssues="Issue"`、`groupMergeRequests="合并请求"`（en 目录仍为 `Issues` / `Merge requests`）。
   - 注：中文目录把 `Issue` 保留为英文（与 `emptyIssue="暂无 Issue"` 一致），因此中文面板出现 `Issue` + `合并请求` 混排——**这是现网真实文案，不是我截图造成的**，如认为不美观请单独开 i18n 单。

2. **搜索框自动聚焦态**：面板打开时会 focus 搜索框（`src/content.js:1713`），新截图中搜索框带蓝色聚焦描边。旧图没有该描边。属真实交互变化，已按现状保留（对商店图观感无负面影响）。

3. **整幅亮度 +4 左右**：新图面板底色约 `(59.5,64.8,73.9)`，旧图约 `(55.2,60.4,68.0)`。这是**采集环境差异**（旧图为 8/21 品牌版 Chrome，本次为 CfT 153），非产品变更，人眼不可辨。

4. **1px 垂直位移**：采集基线差异，非产品变更。

### 2.2 01 与 02 的实测差异

对齐页面主体后（dy=1）只比对面板矩形：

```
01: panel >24 = 6.10%  >64 = 2.83%
02: panel >24 = 5.20%
03: panel >24 = 4.95%
05: panel >24 = 0.69%（徽章区 >24 = 1.44%）
```

热力图 `darkcmp-01..05.png` 显示：**色差全部集中在上述两个分组标题文字上**，其余（列表行、状态徽标、`当前` 标记、`已加载全部 N 条`、搜索框、状态筛选 chip）逐像素一致。肉眼对照见 `side-02.png`、`side-03.png`、`badge-compare-05.png`。

### 2.3 04 为什么必须换（不止文案）

| 行 | 旧 04 | 新 04 |
|----|-------|-------|
| 标题 | GitLab Issue/MR Number Pin | GitLab Issue/MR **编号图钉** |
| 副标题 | 调整列表、缓存和控件交互偏好。 | 调整列表、缓存**与交互控制**偏好。 |
| 默认列表筛选 | Issues 和 Merge requests | Issue **与合并请求** |
| 说明行 | 议题/MR 状态筛选（仅 Open / 全部状态）已移至面板内，可直接在面板中切换。 | Issue/MR 状态筛选（仅 Open / 全部状态）已移动到面板中，请在面板内直接切换。 |
| 勾选行 | 记住列表搜索条件 | 记住列表搜索 |
| **语言** | **不存在** | **存在（简体中文）** ← 结构性过时 |
| 缓存有效期（秒） | 缓存有效期（秒） | 缓存**时长**（秒） |
| 每类最多显示数量 | 每类最多显示数量 | 每类最**大条目数** |
| 渐进加载每批数量 | 可见 | 被新增的语言行挤出首屏 |

`语言` 选项为 8/21 `a6cc832` 引入，旧 04 早于该提交，**已确定过时**。

### 2.4 toast / 空态改动是否影响截图

**结构性不受影响**，逐项说明：
- 本次改动（issue-11 P1：复制 toast、空态、`prefers-reduced-motion`、toast z-index）**都不出现在这 5 个构图里**——5 张图没有任何一次复制操作，03 搜索态有 2 条结果（非空态）。
- 唯一例外是 01/02 里搜索框的聚焦态（见 §2.1.2），已按现状收录。

---

## 3. 已替换交付物

```
screenshots/store/01-main-panel.jpg   1280×800   104,101 B (104.1 KB)   sha256 52f8bfe9…3a2f
```

- 源：`raw-out/zh_CN/dark/01-main-panel.png`（CfT 153 实拍，1280×800 @2x）
- 处理：复用 `scripts/beautify-screenshots.py`（import + 改指向，**脚本本身未改动**），标注文案沿用 `"Quick access from any GitLab page"`
- **扩展名 PNG → JPG**：美化后 PNG 为 213.5 KB，超 200 KB 上限，按 DELIVERY.md 既有压缩策略（「01-02: PNG 过大 → JPEG 85%」）回落为 JPEG。
- 旧文件备份：`review/01-main-panel.BEFORE.png`（106,956 B，sha256 54ae1ef5…b2d8c）
- `preview.html` 第 183 行已同步为 `01-main-panel.jpg`

### 采集可信度
- 扩展副本实测 extId `lenfgcmmohcobbmfipjjeadkkgnnibgl`，`gitlab-reference-navigation-hook` 与 `gitlab-reference-content-scripts` 均注册在 `http://127.0.0.1/*`
- 源码锚点校验（`bae57ac`）：
  - `src/badge-css.js` = `60e0eb32a3f483b54c72d9bac2ad298a1bf5be3249f1d85072c481cd03b03b87`
  - `src/content.js` = `3b9b330351529bd73615e3ee7a96aaf84c4ec79a0f243737ecc73c23b1408c8b`
- 面板状态筛选为面板级持久状态（#16），每次采集前已显式重置为 `仅 Open` 并断言 `rows === 5`，避免跨语言串态

---

## 4. 复核产出物清单（均在 `screenshots/store/review/`）

- `01-main-panel.BEFORE.png` — 被替换的旧图备份
- `01-main-panel.en.png` / `.en-light.png` / `.zh-light.png` — 语言/主题对照（各含同名 `.jpg` 超限件）
- `02-panel-state-filter.zh.jpg` — 02 替换候选（236.2 KB → JPEG 107.1 KB）
- `03-panel-search.zh.png` — 03 替换候选（191.3 KB，未超限）
- `04-options-page.zh-dark.png` — 04 替换候选（97.9 KB）；`04-options-page.zh-light.png` 对照
- `05-badge-scroll.zh.png` — 05 留档（**不建议换**）
- 证据图：`darkcmp-01/02/03/05.png`（对齐后色差热力图）、`side-02.png`、`side-03.png`、`badge-compare-05.png`

---

## 5. 需 manager / admin 决策 / 知悉 —— 裁决结果（2026-09-10 定稿）

> 本节问题已由 manager 裁决，落地情况如下：
> 1. **02、03 一并换 → 采纳**（5 张同批次定稿）：02 已落位为 `02-panel-state-filter.jpg`，03 已就地替换。
> 2. **.jpg 无需处理 → 采纳**：商店截图接受 JPEG 或 24 位 PNG（1280×800），按既有压缩策略走、扩展名与真实字节一致即可。
> 3. 遗留缺陷与 4.（语言口径）维持原判，未在本次处理。
>
> 以下为裁决前的原始记录，保留备查。

1. **02、03 是否一并换**：不换也能用（差异只是分组标题词形），但换掉后与 01 文案一致。倾向：**一起换，5 张同批次文案口径统一**。
2. **01 扩展名变 JPG**：如有流程要求商店资产必须 `.png`，需另行处理（当前按 DELIVERY.md 既有压缩策略执行）。
3. **既有资产遗留缺陷（非本次引入）**：
   - `02-panel-state-filter.png` 实际是 **JPEG 数据挂了 .png 扩展名**（magic `ffd8ff`）。若替换 02，建议顺手修正扩展名。
   - 旧 01（106,956 B）**无法用文档脚本从留档 raw 复现**（`screenshots/raw/01-main-panel.png` 按文档流程跑出 218.1 KB PNG → 104.4 KB JPEG），与 DELIVERY.md「脚本可复用」表述不完全一致。
   - DELIVERY.md 与 `preview.html` 对 04 的描述为「授权区 + 搜索范围下拉可见」，但旧 04 实际可见的是「列表」配置区，**无授权区**；描述与资产不符（文档缺陷，非资产缺陷）。
4. **语言口径**：既有 5 张均为**中文面板**，而商店 Listing 主体为英文。本次沿用中文保持 5 张一致；是否需要整体切英文，属产品决策，请 manager/admin 定，我不擅自改。

---

## 6. 复现方式

```bash
# 采集（扩展副本 + CfT 153 + [::1] 代理），输出 raw-out/<locale>/<theme>/*.png
node .tmp-worktrees-ui/store-shots-bae57ac/store-shots-capture.mjs
# 美化（复用原脚本，仅改 RAW_DIR/STORE_DIR 指向）
python3 .tmp-worktrees-ui/store-shots-bae57ac/beautify-driver.py
```

— UI, 2026-09-10
